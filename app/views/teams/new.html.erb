<!DOCTYPE html>
<html>
  <head>
    <title>panCake!</title>
    <%=render 'layouts/shim'%>
  </head>
  <body class="form-bg">
      <div class="container">
        <div class="form-platform">
          <div class="form">
            <div class="row">
              <h1 class="form-title">Create Team</h1>
            </div>
            <div class="row">
              <div class="col-xs-6 col-xs-offset-3">
                <div class="form-group">

                  <!--Team Creation Form -->
                  <%= form_for(@team) do  |f| %>
                    <%= render 'shared/error_messages', object: f.object%>
                    <!-- Team Name -->
                    <%= f.label :name %>
                    <%= f.text_field :name, class: "form-control" %>

                    <!-- Team Description -->
                    <%= f.label :description %>
                    <%= f.text_area :description, class: "form-control", size: "20x5" %>

                    <!-- Submit Button -->
                    <%= f.submit "Add", class: "btn btn-primary" %>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

  </body>



</html>